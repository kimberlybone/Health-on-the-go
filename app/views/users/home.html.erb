<ul>
<li>
  This is the Home Page <br>
  <% if @logged_in_user %>
  I am already a user
  <% else %>
  <%= link_to "Login", new_login_path %>
  <%= link_to "Sign Up", new_user_path %>
  <% end %>
</li>
</ul>
